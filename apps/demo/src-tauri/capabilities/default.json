{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default capability set for the demo application with scoped permissions",
  "windows": ["main"],
  "permissions": ["core:default"]
}
